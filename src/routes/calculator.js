import Calculator from '../components/calculator';

const CalculatorSection = () => (
  <div className="container calc-container">
    <h2 className="section-title">Let&apos;s do some maths</h2>
    <Calculator />
  </div>
);
export default CalculatorSection;
